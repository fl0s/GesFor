{% extends 'admin/base.html.twig' %}

{% set page = "admin.admin.type_formation" %}
{% set pageHeader = "Gestion des types de formation" %}

{% block body %}
    <div id="wrapper">
        <div id="container">

            <div class="box">
                <div class="box-header">
                    <div class="box-tools pull-right">
                        <a href="{{ path("admin.type.create") }}" class="btn btn-success btn-sm">Ajouter</a>
                    </div>
                </div>
                <div class="box-body no-padding">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Intitulé</th>
                                <th width="150px">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for type in types %}
                            <tr>
                                <td>{{ type.titre }}</td>
                                <td>
                                    <a href="{{ path('admin.antenne.edit', {'id': type.id}) }}" class="btn btn-warning btn-xs">Editer</a>
                                </td>
                            </tr>
                        {% else %}
                            <tr class="info"><td colspan="3">Aucun types de formation</td></tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
{% endblock %}

{% block stylesheets %}
<style>

</style>
{% endblock %}
